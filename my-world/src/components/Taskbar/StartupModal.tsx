export const StartupModal = (isOpen: {}) => {
  return (
    <section className="Startup-Modal">
      {" "}
      <div>This is new Modal</div>
      <button onClick={(e) => e.preventDefault()}>안여론애뢔ㅑ</button>
    </section>
  );
};
